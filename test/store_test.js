var expect = require('chai').expect;
var lookupKeyword = require('../store');

describe('lookupKeyword', function () {
  it('should lookup keyword in the list of products', function () {
    
    // 1. ARRANGE
    var keyword = "mattress";

    // 2. ACT
    var response = request;

    // 3. ASSERT
    expect(response).to.be.equal(sum1);

  });
});